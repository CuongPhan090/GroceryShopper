<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groceryshopper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_shopping_basket"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroceryShopper">
        <activity android:name=".views.ShippingPaymentActivity"></activity>
        <activity android:name=".views.ForgotPasswordActivity"></activity>
        <activity android:name=".views.ResetPasswordActivity" />
        <activity android:name=".views.UserDetailActivity" />
        <activity android:name=".views.UserProfileActivity" />
        <activity android:name=".views.CartActivity" />
        <activity android:name=".views.ProductDetailActivity" />
        <activity android:name=".views.ProductActivity" />
        <activity android:name=".views.SubCategoryActivity" />
        <activity
            android:name=".views.CategoryActivity"
            android:theme="@style/Theme.GroceryShopper.Overlay" />
        <activity android:name=".views.RegisterActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>